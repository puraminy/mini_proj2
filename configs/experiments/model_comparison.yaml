experiment:
  id: E04
  name: model_comparison
results:
  root: results
# Synthetic fixture keeps CI deterministic; replace dataset.path in real runs.
dataset:
  name: manuscript_fixture
  size: 24
  seed: 7
prompt:
  template: 'Classify: {text}'
  n_shot: 0
model:
  name: reference-model
  score_scale: 0.9
depthrank:
  threshold: 0.5
mi:
  mask_fraction: 0.15
statistics:
  bootstrap_samples: 50
  seed: 13
